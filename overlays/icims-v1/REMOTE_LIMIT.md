# Remote limitation

The iCIMS overlay is complete and locally verified. Remote end-to-end verification remains intentionally blocked until `autonomy/workday-v1` exposes the complete Workday source tree rather than only its historical overlay shell. This file records that boundary so a red materialization check is not mistaken for an iCIMS code failure.
