# iCIMS redaction guarantees

Target evidence stores host, normalized tenant, job identifier, route, and a query-free URL only. Credential evidence stores only `icims/<profile>`. Candidate answers and entered values are not written into the wizard journal. Screenshots use the shared candidate-text redaction path.
