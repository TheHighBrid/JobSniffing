# Next task

After the complete Workday source is materialized and this iCIMS overlay passes remote verification, the next sequential task is **SmartRecruiters submission and discovery v1**.

Do not rebuild the credentials loader, DOM controls, wizard, gates, scheduler, or existing ATS adapters.
